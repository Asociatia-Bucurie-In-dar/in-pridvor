'use client'

import React from 'react'

interface PageClientProps {
  postId: number
}

const PageClient: React.FC<PageClientProps> = ({ postId: _postId }) => {
  return <React.Fragment />
}

export default PageClient
